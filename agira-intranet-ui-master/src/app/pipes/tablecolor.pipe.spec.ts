import { TablecolorPipe } from './tablecolor.pipe';

describe('TablecolorPipe', () => {
  it('create an instance', () => {
    const pipe = new TablecolorPipe();
    expect(pipe).toBeTruthy();
  });
});
